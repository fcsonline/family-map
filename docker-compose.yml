services:
  family-map:
    build:
      context: .
      args:
        VITE_DATA_MODE: self-hosted
    ports:
      - "8080:3001"
    environment:
      - PORT=3001
      - DATA_DIR=/data
    volumes:
      - family-map-data:/data
volumes:
  family-map-data:
