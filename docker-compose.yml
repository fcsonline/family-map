version: "3.9"
services:
  family-map:
    build: .
    ports:
      - "8080:3001"
    environment:
      - PORT=3001
      - DATA_DIR=/data
    volumes:
      - family-map-data:/data
volumes:
  family-map-data:
